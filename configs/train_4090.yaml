data_root: "./data"
img_size: 256
batch_size: 8
num_workers: 4
epochs: 100
lr: 0.0001
weight_decay: 0.0001
amp: true
save_interval: 5
vis_interval: 5
device: "cuda"

loss_weights:
  lambda_intensity: 1.0
  lambda_gradient: 5.0
  lambda_color: 1.0
  lambda_thermal: 2.0
